# Tool Contracts
All tools validate structured input and output, redact credentials, enforce timeouts and scope, persist concise observable summaries, and return truthful failures. Model text is never interpolated into shell commands.
